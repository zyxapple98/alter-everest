# Canonical expedition proofs

The reducer copies the exact admitted candidate bytes here after a successful
replay. The SHA-256 digest of those bytes is recorded in the matching event and
signed receipt.

Files in this directory are reducer output. Expedition pull requests must not
edit them.
