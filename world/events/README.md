# Canonical expedition events

This directory contains compact, immutable records produced by the trusted
reducer. Full candidate proofs, physics artifacts, snapshots, and animation
traces live in content-addressed object storage.

One accepted expedition produces one event and one canonical event commit.
Contributor pull-request branches are proposals; event order is canonical.
